apiVersion: v1
kind: Service
metadata:
  name: account-service
spec:
  selector:
    app: account-service
    version: blue # Traffic defaults to blue initially
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080