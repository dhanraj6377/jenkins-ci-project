apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service-blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-service
      version: blue
  template:
    metadata:
      labels:
        app: account-service
        version: blue
    spec:
      containers:
      - name: account-service
        image: your-dockerhub-user/account-service:IMAGE_TAG_PLACEHOLDER
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: docker